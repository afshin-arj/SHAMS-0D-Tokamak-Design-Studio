id: engineering_divertor_proxy_v2
name: Divertor heat exhaust proxy with tech modes
version: '0.2'
type: physics_proxy
assumptions:
- Parallel heat flux computed from SOL power and wetted area proxy
- Tech modes adjust allowable limit by multipliers
validity:
  lambda_q_m:
  - 0.001
  - 0.02
outputs:
- q_parallel_MW_per_m2
- q_parallel_limit_MW_per_m2
maturity:
  trl: 4
  envelope: baseline
  notes: Default maturity tag added in v35; update per proxy.
