# PATCH NOTES — v83 (2026-01-08)

v83 is a **PROCESS-compatibility additive release**.

## Added (no behavior change)
- Feasible Scan export (feasible/infeasible tagging, constraint margins, active constraints)
- Feasible Pareto filtering (feasible-only, user-declared objectives as metadata)
- SHAMS→PROCESS handoff envelope export (empirical envelope + constraint set hash)
- Documentation bundles from v80 (governance, narrative, methods outline) and v81–v83 planning specs

## Not changed
- Physics evaluator
- Solver logic / continuation behavior
- Defaults or UI behavior
