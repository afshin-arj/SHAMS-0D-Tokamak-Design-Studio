# Stage 2 — Scenario comparison

Adds `src/scenarios` and `tools/run_scenarios.py` to replay a design point across multiple scenario assumptions.

Scenarios can override selected `PointInputs` fields and/or lifecycle economics assumptions (discount rate, availability, etc.).
