{
  "schema": "feasible_first_surrogate_contract.v1",
  "non_authoritative": true,
  "purpose": "Feasible-first surrogate acceleration used only to propose candidate points; all proposals must be verified by frozen truth.",
  "defaults": {
    "training_min_rows": 16,
    "feasibility_margin_key": "min_margin_frac",
    "objective_key": "score",
    "objective_sense": "max",
    "pool_multiplier": 50,
    "pool_min": 64,
    "kappa": 0.5,
    "ridge_alpha": 0.001
  }
}
