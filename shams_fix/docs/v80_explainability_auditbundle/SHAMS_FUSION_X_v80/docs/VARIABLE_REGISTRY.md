# Variable Registry

SHAMS maintains a PROCESS-style variable registry to keep the tool auditable as models evolve.

Each variable entry provides:
- key (output name)
- units
- meaning
- model/source module

In the UI, open the **Variable Registry** tab to search and export the registry.
