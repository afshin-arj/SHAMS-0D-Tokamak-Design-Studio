# PATCH NOTES — v86.1 (2026-01-08)

Hotfix release for UI indentation/syntax integrity.

## Fixed
- Streamlit UI: Optimization Sandbox tab indentation error (IndentationError) resolved by rewriting the tab block.

## Verified
- Python compilation check across package (compileall).

## Unchanged
- Physics models
- Solver logic / continuation behavior
- Constraints / defaults
